package velo.actions.tools;


public class FacadeScriptingTools extends CoreActionTools {
	
	
	/*
	AccountManagerRemote accountManager;
	UserManagerRemote userManager;
	
	public AccountManagerRemote getAccountManager() throws NamingException {
		try {
			if (accountManager == null) {
				Context ic = new InitialContext();
				accountManager = (AccountManagerRemote) ic.lookup("velo/AccountBean/remote");
				
				return accountManager;
			}
			else {
				return accountManager;
			}
		} catch (NamingException e) {
			throw e;
		}
	}
	
	public UserManagerRemote getUserManager() throws NamingException {
		try {
			if (userManager == null) {
				Context ic = new InitialContext();
				userManager = (UserManagerRemote) ic.lookup("velo/UserBean/remote");
				
				return userManager;
			}
			else {
				return userManager;
			}
		} catch (NamingException e) {
			throw e;
		}
	}
	*/
	
	
	
	
}
